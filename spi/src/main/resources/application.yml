spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: true

  h2:
    console:
      enabled: true

  flyway:
    enabled: false

  jmx:
    enabled: false
  main:
    lazy-initialization: true
    web-application-type: reactive

spi:
  default-initial-balance: 1_000_000_000

server:
  port: 8001