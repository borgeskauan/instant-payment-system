syntax = "proto3";

option java_package = "br.kauan.notificationgateway.grpc.proto";
option java_outer_classname = "NotificationProto";
option java_multiple_files = true;

package notification;

service NotificationGateway {
  rpc StreamNotifications(stream ClientMessage) returns (stream Notification);
}

message ClientMessage {
  oneof message {
    Ack ack = 1;
  }
}

message Ack {
  string delivery_id = 1;
}

// A single notification forwarded from the internal Kafka topic.
// - delivery_id: reserved for reliable delivery/ACK support; empty for now.
// - payload: raw notification body. For the current API version this is
//            application/json; charset=utf-8, but the gateway treats it as
//            opaque bytes and does not parse or validate the body.
message Notification {
  string delivery_id = 1;
  bytes payload = 2;
}
