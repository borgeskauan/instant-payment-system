info:
  name: 05 Send 25.50 from Alice to Bob
  type: http
  seq: 1

http:
  method: post
  url: "{{senderPspUrl}}/transfer/execute"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "senderCustomerId": "{{senderCustomerId}}",
        "receiverPixKey": "{{receiverPixKey}}",
        "amount": 25.50,
        "description": "Bruno transfer flow test"
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
